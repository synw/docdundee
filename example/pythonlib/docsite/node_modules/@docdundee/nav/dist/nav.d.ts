import { useApi } from "restmix";
import { DirNavListing, RouteDataPayload } from "./interfaces";
import { useDocloader } from "./loader";
declare const useNav: (docloader: ReturnType<typeof useDocloader>, api: ReturnType<typeof useApi>) => {
    readonly tree: DirNavListing;
    init: () => Promise<void>;
    loadFromRoutePath: (routePath: string) => Promise<RouteDataPayload>;
    findNode: (routePath: string) => DirNavListing;
};
export { useNav };
